import React from 'react';
import './Home.scss';
import Loading from '../../common/Loading/Loading';

const Home = (props) => {
    return (
        <main className='home'></main>
    );
};

export default Home;
