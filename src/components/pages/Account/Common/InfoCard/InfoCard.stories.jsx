import InfoCard from './InfoCard';

export default {
    title: 'Lk/InfoCard',
    component: InfoCard,
};

export const Default = {
    args: { type: 'default' },
};

export const Full = {
    args: { type: 'full' },
};
